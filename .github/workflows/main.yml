name: HelloBasicGithubAction

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo Hello~ 
      - name: List
              ls -al
      - name: version
              python -v
              
