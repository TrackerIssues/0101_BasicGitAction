name: HelloBasicGithubAction

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo Hello~ 
              ls -al
              python -v
              
